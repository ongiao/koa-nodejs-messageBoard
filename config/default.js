// root@localhost:3306
// jdbc:mysql://localhost:3306/?user=root

const config = {
    port: 3000,
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: '2301226',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;